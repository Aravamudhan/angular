// For an app, a module is created and controllers are attached to it.
// The empty array is the place where providers, if any, are registerd.
// These providers would create an expose APIs for their related services when
// requested.
angular.module('phoneListModule', []);
